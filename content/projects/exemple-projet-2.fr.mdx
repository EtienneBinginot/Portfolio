## En détail

[placeholder] Ce second projet illustre le cas d'une fiche à une seule
métrique et sans graphique : le chiffre clé reste visible plus haut, dans la
section Chiffres clés, donc ce write-up reste une prose continue plutôt que
de le reprendre en `InlineMetric`.

[placeholder] C'est ici que vient le détail que les champs fixes de la fiche
(`problem`, `decisions`) ne peuvent pas porter sans devenir illisibles : le
contexte complet, les pistes explorées puis abandonnées, et ce qui a
finalement fait pencher la balance vers l'arbitrage retenu.
