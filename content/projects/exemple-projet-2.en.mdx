## Writing note

This project has no chart in its data, only a metric already shown in the
numbers section above — so this write-up stays plain prose, with no inline
component, to avoid duplicating a metric that's already visible higher on
the page.

The long-form content here mostly details context and the alternatives
that were ruled out, which doesn't fit in the `decisions` field without
becoming unreadable.
