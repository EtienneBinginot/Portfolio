## In detail

[placeholder] This second project illustrates a fact sheet with a single
metric and no chart: the key figure is already visible higher up, in the
key figures section, so this write-up stays plain prose instead of
repeating it through `InlineMetric`.

[placeholder] This is where the detail goes that the fact sheet's fixed
fields (`problem`, `decisions`) can't carry without becoming unreadable: the
full context, the paths explored and then dropped, and what finally tipped
the balance toward the chosen trade-off.
